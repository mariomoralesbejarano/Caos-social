import { useArenaAction, useGetBarajaRoom, useLeaveBarajaRoom } from "@workspace/api-client-react";
import type { ArenaState } from "@workspace/api-client-react";
import { useRouter } from "expo-router";
import React, { useEffect, useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import BoardRoomLobby from "@/components/BoardRoomLobby";
import { useColors } from "@/hooks/useColors";
import { clearBarajaSession, loadBarajaSession, type BarajaSession } from "@/lib/barajaSession";

const ROUND_META = {
  reflejos: { title: "Duelo de Reflejos", hint: "Pulsa cuando aparezca el flash", color: "#ff4f9a" },
  bomba: { title: "Bomba de Tiempo", hint: "Pasa el artefacto antes de que explote", color: "#ffb84d" },
  memoria: { title: "Memoria Rápida", hint: "Repite la secuencia y suma puntos", color: "#64e6a5" },
} as const;

export default function ArenaScreen() {
  const colors = useColors(); const insets = useSafeAreaInsets(); const router = useRouter();
   const [session, setSession] = useState<BarajaSession | null>(null); const [hydrated, setHydrated] = useState(false); const [error, setError] = useState<string | null>(null); const [now, setNow] = useState(() => Date.now());
  useEffect(() => { loadBarajaSession().then((value) => { setSession(value); setHydrated(true); }); }, []);
   useEffect(() => { const timer = setInterval(() => setNow(Date.now()), 250); return () => clearInterval(timer); }, []);
  const { data: room, isLoading } = useGetBarajaRoom(session?.roomCode ?? "", session?.playerId ?? "");
  const scoreMut = useArenaAction(); const leaveMut = useLeaveBarajaRoom();
  const gs = room?.gameState?.type === "arena" ? room.gameState as ArenaState : null; const myId = session?.playerId ?? "";
   async function play(action: "tap" | "pass-bomb" | "memory-input", points?: number, value?: number) { if (!session) return; setError(null); try { await scoreMut.mutateAsync({ code: session.roomCode, playerId: myId, action, points, value }); } catch (cause) { setError(cause instanceof Error ? cause.message : String(cause)); } }
  async function leave() { if (session) await leaveMut.mutateAsync({ code: session.roomCode, playerId: myId }); await clearBarajaSession(); router.replace("/"); }
  if (!hydrated || (session && isLoading)) return <View style={styles.loading}><ActivityIndicator color="#ff4f9a" size="large" /></View>;
  if (!session || !room) return <BoardRoomLobby kind="arena" title="Arena de Minijuegos" subtitle="Rondas cortas, reflejos y marcador grupal en directo." accent="#ff4f9a" maxPlayers={8} defaultMaxPlayers={4} onBack="/" />;
  if (!gs) return <View style={styles.loading}><ActivityIndicator color="#ff4f9a" size="large" /></View>;
   const meta = ROUND_META[gs.roundType]; const leader = Object.entries(gs.scores).sort((a, b) => b[1] - a[1])[0]?.[0]; const flashReady = gs.roundType === "reflejos" && gs.flashAt !== null && now >= gs.flashAt; const bombRemaining = gs.bombDeadline ? Math.max(0, gs.bombDeadline - now) : 0; const myMemoryInput = gs.memoryInput[myId] ?? []; const sequenceVisible = gs.roundType === "memoria" && now - gs.roundStartedAt < 2200;
  return <View style={[styles.screen, { backgroundColor: colors.background }]}><ScrollView contentContainerStyle={[styles.container, { paddingTop: insets.top + 16, paddingBottom: insets.bottom + 28 }]}>
    <View style={styles.header}><View><Text style={[styles.kicker, { color: meta.color }]}>ARENA · RONDA {Math.min(gs.round, 9)}/9</Text><Text style={[styles.title, { color: colors.foreground }]}>Playus de barrio</Text></View><Pressable onPress={leave} style={styles.out}><Text style={styles.outText}>SALIR</Text></Pressable></View>
     <View style={[styles.challenge, { borderColor: meta.color, backgroundColor: `${meta.color}18` }]}>
       <Text style={[styles.challengeTitle, { color: meta.color }]}>{meta.title}</Text>
       <Text style={styles.challengeHint}>{meta.hint}</Text>
       {gs.roundType === "reflejos" && <View style={[styles.flash, { backgroundColor: flashReady ? meta.color : "#2a183e", borderColor: meta.color }]}><Text style={styles.flashText}>{flashReady ? "AHORA" : "..."}</Text></View>}
       {gs.roundType === "bomba" && <View style={styles.bombPanel}><Text style={styles.bombLabel}>{gs.bombHolder === myId ? "LA BOMBA ESTÁ CONTIGO" : `LA BOMBA ESTÁ CON ${room.players.find((p) => p.id === gs.bombHolder)?.name ?? "OTRO"}`}</Text><Text style={[styles.bombTimer, { color: bombRemaining < 1000 ? "#ff4f9a" : meta.color }]}>{(bombRemaining / 1000).toFixed(1)}s</Text></View>}
       {gs.roundType === "memoria" && <View style={styles.sequencePanel}><Text style={styles.sequenceLabel}>{sequenceVisible ? "MEMORIZA LA SECUENCIA" : `TU PROGRESO · ${myMemoryInput.length}/${gs.memorySequence.length}`}</Text><View style={styles.sequenceRow}>{gs.memorySequence.map((value, index) => <View key={`${value}-${index}`} style={[styles.sequenceDot, { backgroundColor: sequenceVisible || index < myMemoryInput.length ? meta.color : "#3b2850" }]}><Text style={styles.sequenceDotText}>{sequenceVisible ? value + 1 : index < myMemoryInput.length ? "✓" : "?"}</Text></View>)}</View></View>}
       {gs.roundType === "reflejos" && <Pressable onPress={() => play("tap", 3)} disabled={!flashReady || scoreMut.isPending} style={[styles.playButton, { backgroundColor: meta.color, opacity: flashReady ? 1 : .5 }]}><Text style={styles.playText}>¡TOCAR!</Text></Pressable>}
       {gs.roundType === "bomba" && <Pressable onPress={() => play("pass-bomb")} disabled={gs.bombHolder !== myId || scoreMut.isPending} style={[styles.playButton, { backgroundColor: meta.color, opacity: gs.bombHolder === myId ? 1 : .5 }]}><Text style={styles.playText}>PASAR BOMBA</Text></Pressable>}
       {gs.roundType === "memoria" && <View style={styles.memoryButtons}>{[0, 1, 2, 3].map((value) => <Pressable key={value} onPress={() => play("memory-input", 4, value)} disabled={!sequenceVisible && myMemoryInput.length >= gs.memorySequence.length || scoreMut.isPending} style={[styles.memoryButton, { borderColor: meta.color }]}><Text style={[styles.memoryButtonText, { color: meta.color }]}>{value + 1}</Text></Pressable>)}</View>}
     </View>
     <View style={styles.scoreCard}><View style={styles.scoreHeader}><Text style={styles.section}>MARCADOR EN DIRECTO</Text><Text style={styles.leader}>LÍDER · {room.players.find((p) => p.id === leader)?.name ?? "—"}</Text></View>{[...gs.playerOrder].sort((a, b) => (gs.scores[b] ?? 0) - (gs.scores[a] ?? 0)).map((id, index) => <View key={id} style={styles.scoreRow}><Text style={[styles.rank, { color: index === 0 ? "#ffcf62" : "#7d98aa" }]}>{index + 1}</Text><Text style={styles.player}>{room.players.find((p) => p.id === id)?.name ?? "Jugador"}</Text><Text style={[styles.points, { color: meta.color }]}>{gs.scores[id] ?? 0}</Text></View>)}</View>
    {gs.phase === "ended" && <Text style={styles.winner}>GANADOR · {room.players.find((p) => p.id === gs.winnerId)?.name ?? "Jugador"}</Text>}{error && <Text style={styles.error}>{error}</Text>}
  </ScrollView></View>;
}

const styles = StyleSheet.create({
   screen: { flex: 1 }, loading: { flex: 1, alignItems: "center", justifyContent: "center", backgroundColor: "#0a0014" }, container: { width: "100%", maxWidth: 720, alignSelf: "center", paddingHorizontal: 16, gap: 16 }, header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, kicker: { fontFamily: "Inter_700Bold", fontSize: 10, letterSpacing: 1.7 }, title: { fontFamily: "Inter_700Bold", fontSize: 28, marginTop: 5 }, out: { borderWidth: 1, borderColor: "#3d2b55", borderRadius: 9, padding: 10 }, outText: { color: "#9a8fb8", fontFamily: "Inter_700Bold", fontSize: 11 }, challenge: { borderWidth: 2, borderRadius: 20, padding: 22, alignItems: "center", gap: 7 }, challengeTitle: { fontFamily: "Inter_700Bold", fontSize: 23, textAlign: "center" }, challengeHint: { color: "#c9bddb", fontFamily: "Inter_400Regular", fontSize: 12, textAlign: "center" }, flash: { width: 110, height: 110, borderRadius: 55, marginVertical: 12, borderWidth: 2, alignItems: "center", justifyContent: "center", shadowColor: "#fff", shadowOpacity: .65, shadowRadius: 25 }, flashText: { color: "#fff", fontFamily: "Inter_700Bold", fontSize: 12, letterSpacing: 1 }, bombPanel: { alignItems: "center", paddingVertical: 14, gap: 3 }, bombLabel: { color: "#f4eafa", fontFamily: "Inter_700Bold", fontSize: 11, letterSpacing: .7, textAlign: "center" }, bombTimer: { fontFamily: "Inter_700Bold", fontSize: 44 }, sequencePanel: { alignItems: "center", paddingVertical: 14, gap: 10 }, sequenceLabel: { color: "#d9c9e9", fontFamily: "Inter_700Bold", fontSize: 10, letterSpacing: 1, textAlign: "center" }, sequenceRow: { flexDirection: "row", gap: 8 }, sequenceDot: { width: 38, height: 38, borderRadius: 19, alignItems: "center", justifyContent: "center" }, sequenceDotText: { color: "#190b26", fontFamily: "Inter_700Bold", fontSize: 14 }, memoryButtons: { flexDirection: "row", gap: 9, marginTop: 8 }, memoryButton: { width: 50, height: 50, borderRadius: 12, borderWidth: 2, alignItems: "center", justifyContent: "center", backgroundColor: "#1a0833" }, memoryButtonText: { fontFamily: "Inter_700Bold", fontSize: 18 }, playButton: { minHeight: 52, borderRadius: 10, minWidth: 190, alignItems: "center", justifyContent: "center" }, playText: { color: "#14091c", fontFamily: "Inter_700Bold", letterSpacing: 1.2 }, scoreCard: { borderRadius: 15, borderWidth: 1, borderColor: "#2a1450", backgroundColor: "#15042a", padding: 14, gap: 8 }, scoreHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 4 }, section: { color: "#f5f5ff", fontFamily: "Inter_700Bold", fontSize: 13 }, leader: { color: "#ffcf62", fontFamily: "Inter_700Bold", fontSize: 9 }, scoreRow: { minHeight: 43, borderRadius: 8, backgroundColor: "#1a0833", flexDirection: "row", alignItems: "center", paddingHorizontal: 9, gap: 10 }, rank: { width: 18, fontFamily: "Inter_700Bold", textAlign: "center" }, player: { color: "#f5f5ff", fontFamily: "Inter_600SemiBold", fontSize: 13, flex: 1 }, points: { fontFamily: "Inter_700Bold", fontSize: 18 }, winner: { color: "#64e6a5", fontFamily: "Inter_700Bold", textAlign: "center", letterSpacing: 1.5 }, error: { color: "#ff2d6f", fontFamily: "Inter_600SemiBold", textAlign: "center" },
});